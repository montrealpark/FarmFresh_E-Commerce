package models.action;

public class information {
    //get request to ask the manager to get it.
    // get package
    //get item
    //mostly one line, except login
    // just to move the code from controller, but in the controller is still have some validation code
    // just all the action that you can do with buttons

    // in the controller, it's all servlet
}
